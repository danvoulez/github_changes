# Variáveis de Ambiente para PromptOS v1.0

# -- Configuração da Aplicação --
# LOG_LEVEL=DEBUG  # Opcional: DEBUG, INFO, WARNING, ERROR (padrão INFO)

# -- Chaves de API --
GEMINI_API_KEY="AIzaSy..............." # Sua chave da API Google Gemini (OBRIGATÓRIO)
# GITHUB_PAT="ghp_.................."  # Seu GitHub Personal Access Token (com permissão 'repo' se for clonar privados) (OPCIONAL, mas recomendado para evitar rate limit e clonar privados)

# -- Outras configurações do AgentOS (se aplicável) --
# MONGODB_URI="mongodb+srv://..."
# REDIS_URL="redis://..."
# SECRET_KEY="uma_chave_muito_secreta"
